# Single Spa Child

### Setup
    ``` npm install ```

### Run - standalone with test component

1. Start Application using ``` npm run start:standalone ``` to run the application in standalone mode
2. Start Test component by ``` cd ../mfe-component && npm run start ```
2. Visit ```http://localhost:8500``` to see application with test component running